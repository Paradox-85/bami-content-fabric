## Review
- Correct: `scripts/media_library.py:311-319` теперь сохраняет Envato-injected classification при повторном проходе `media_library.classify()`. Это закрывает r3 blocker #1; поведение дополнительно покрыто интеграционным тестом `tests/test_envato_assets/test_pipeline.py:523-645`. Я также прогнал `pytest tests/test_envato_assets/test_pipeline.py tests/test_media_library.py -q` — `53 passed`.
- Correct: для r3 blocker #3 логика калибровки теперь считает review-rate только по calibration sample (`tools/envato_assets/cli.py:478-486`) и перед rerun целенаправленно чистит старые sample rows по `pack_slug` (`tools/envato_assets/cli.py:369-386`), что соответствует требованию не накапливать stale rows между повторами.
- Fixed: не применял — review-only.
- Blocker: исправление rerun в `tools/envato_assets/cli.py:374-386` ломает документированный режим `--skip-extract` (`tools/envato_assets/cli.py:337`). Сейчас stale rows удаляются **до** ветки `if not skip_extract`, а затем оценка берёт данные из уже очищенного `crop_index` (`tools/envato_assets/cli.py:478-486`). В результате `calibrate --skip-extract` фактически оценивает пустой/неполный sample вместо «existing crops». Это regression, которую нужно устранить перед принятием: либо не purge-ить sample rows в `--skip-extract` режиме, либо считать threshold по копии/изолированному view без разрушения существующего индекса.
- Note: новый bridge/integration test действительно ловит старый баг, потому что специально использует crop id с `kpi-dashboard`, который раньше был бы переопределён keyword-классификацией в `kpi`; теперь тест проверяет, что остаётся `comparison` после `inventory -> classify` (`tests/test_envato_assets/test_pipeline.py:555-641`).