{
  "id": "checklist-status",
  "vue_component": "ChecklistStatus",
  "category_id": "checklist-status",
  "title": "Checklist / status tracker",
  "description": "Status checklist with per-item state (done, pending, in-progress, blocked) and optional assignee.",
  "current_runtime_kind": "list",
  "implemented": false,
  "props": [
    {
      "name": "items",
      "type": "Array",
      "required": true,
      "default": null,
      "description": "Checklist items: [{label, status: 'done'|'pending'|'in_progress'|'blocked', assignee?}]."
    },
    {
      "name": "columns",
      "type": "Number",
      "required": false,
      "default": 1,
      "description": "Number of columns (1 or 2). Clamp to [1,2]."
    }
  ]
}
