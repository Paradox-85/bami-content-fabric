## Review
- Correct: Паритет schema/build для block kinds теперь реально закрыт: `shared/pptx/blocks.py:927-951` регистрирует все новые builders, а `tests/test_schema_sync.py:10-24` проверяет точное совпадение enum из schema с `BUILDERS`.
- Correct: Путь с notes-hint реализован end-to-end: генератор пишет hint в notes (`shared/pptx/build.py:166-167`), валидатор читает hint из notes/XML раньше эвристик (`tools/pptx_validate/cli.py:48-65`), а тесты проверяют наличие hint и успешную валидацию собранной колоды (`tests/test_blocks_new.py:60-72`).
- Correct: `image.fit` больше не фиктивный контракт: `shared/pptx/blocks.py:288-384` реализует `contain`, `cover` и `fill`; sample/fixtures теперь действительно гоняют новый primitive через `tags` и отдельный fixture deck (`clients/_sample/deck.json:47-58`, `tests/test_blocks_new.py:31-39`).
- Correct: Я перепроверил состояние репозитория командами `python -m pytest -q` и `python -m tools.pptx_gen --schema clients/_sample/deck.json --out .pi/temp/review-r2-out.pptx && python -m tools.pptx_validate .pi/temp/review-r2-out.pptx`; локально всё зелёное (`21 passed`, validator `OK`).
- Blocker: Миграция `schema_version: 1` не реализована, хотя план и комментарий в коде это обещают. В `shared/pptx/schema.py:103-123` обновляется только `cur == 0`; явный `schema_version == 1` проходит без апгрейда и остаётся версией 1. Тест покрывает только deck без `schema_version`, но не version 1 (`tests/test_migrations.py:11-24`). Локально воспроизвёл: `load_deck()` возвращает `schema_version == 1` для deck с `"schema_version": 1`.
- Note: Более широкий approved plan всё ещё не завершён в текущем состоянии репозитория: schema принимает `layout`/`variant`/`content`, но `shared/pptx/build.py:174-179` пока оставляет layout expansion как `pass`. Если эта проверка относится только к r2-corrections, это follow-up, а не регрессия данного changeset.
