## Review
- Correct: Базовый каркас по плану действительно добавлен: пакет `tools/envato_assets/` присутствует, bridge-директория `_envato_ingest` учитывается в `scripts/media_library.py:184-198`, а smoke/targeted tests проходят (`python -m pytest tests/test_envato_assets/test_pipeline.py -q` → `42 passed`, `python -m pytest tests/test_media_library.py -q` → `9 passed`). CLI тоже поднимается (`python -m tools.envato_assets --help`).
- Fixed: Нет — review-only.
- Blocker: Интеграция классификации Envato в существующий media-library workflow не завершена. `extract` и `classify` сохраняют Envato-метаданные только в `_crop_index.json` (`tools/envato_assets/cli.py:279-303`, `tools/envato_assets/cli.py:464-474`), но `handoff` просто перезапускает `media_library.inventory/classify/...` (`tools/envato_assets/cli.py:510-525`). При этом `media_library.inventory` создает новые manifest entries только из файловой системы без `slot_count/source_pack/source_ref/envato_crop_id` (`scripts/media_library.py:184-198`, `scripts/media_library.py:430-460`), а `media_library.classify_entry` переклассифицирует asset только по имени файла/паттернам (`scripts/media_library.py:269-301`). Практическая проверка на временном media root показала, что `_envato_ingest/test-pack-p1-cc1.png` попадает в manifest без Envato-полей и классифицируется как `uncategorized`. Это противоречит Task 4/5 и главной цели плана: финальная библиотека не использует уже рассчитанную Envato-классификацию.
- Blocker: `calibrate` не реализует заявленный gate. Вместо сохранения/оценки calibration crops команда только печатает crop plans (`tools/envato_assets/cli.py:363-389`), а затем считает review rate по уже существующему `crop_index` (`tools/envato_assets/cli.py:391-395`). На чистом запуске это легко дает `0` crops и ложный `OK`, то есть stop-condition из Task 7 фактически не работает.
- Note: В `classify` неверно подтягивается discovery metadata: `load_discovery_index()` возвращает словарь, ключованный по `item_url` (`tools/envato_assets/extract.py:55-118`), но `cli.classify()` ищет запись по `source_zip` (`tools/envato_assets/cli.py:444-447`). Из-за этого до `classify_crop()` не доходят поля вроде `estimated_pattern_count`, и rich metadata/slot heuristics работают не по плану.
- Note: QA contact-sheet workflow не подключен к pipeline. `build_contact_sheet()` реализован (`tools/envato_assets/qa.py:53-120`) и импортирован (`tools/envato_assets/cli.py:67-72`), но не вызывается ни из `catalog`, ни из `handoff`, ни из `full` (`tools/envato_assets/cli.py:486-501`, `tools/envato_assets/cli.py:553-580`). Значит Task 8 и обещанный артефакт `_qa_contact_sheet.png` сейчас не выполняются end-to-end.
- Note: `_processing_report.md` пишется с плейсхолдером `Generated: TODO` вместо реального timestamp (`tools/envato_assets/catalog.py:246-250`).
- Note: Тесты покрывают в основном unit-level и synthetic checks; реальный bridge не проверяется. `TestHandoffSchema` лишь валидирует вручную собранный dict (`tests/test_envato_assets/test_pipeline.py:357-380`), но не вызывает настоящий `handoff` и потому не ловит потерю Envato-метаданных/классификации.