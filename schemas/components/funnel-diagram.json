{
  "id": "funnel-diagram",
  "vue_component": "FunnelDiagram",
  "category_id": "funnel-diagram",
  "title": "Funnel / pipeline diagram",
  "description": "Visual funnel with narrowing stages. Each stage shows a label and value. Supports highlighting.",
  "current_runtime_kind": "diagram",
  "implemented": false,
  "props": [
    {
      "name": "stages",
      "type": "Array",
      "required": true,
      "default": null,
      "description": "Funnel stages from wide (top) to narrow (bottom): [{label, value, color?, highlight?}]."
    },
    {
      "name": "accent",
      "type": "String",
      "required": false,
      "default": "primary",
      "enum": ["primary", "primary_dark", "positive", "negative", "warning", "neutral"],
      "description": "Default color token for stage fills."
    }
  ]
}
