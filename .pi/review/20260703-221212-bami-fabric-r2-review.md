## Review
- Correct: `docs/runbooks/generate-deck.md:5-19` now truthfully documents the repo-root requirement and manual dependency installation, and explicitly warns that `pip install -e .` does not work yet. Verified by `python -m pip install -e . --dry-run`, which still fails with `Multiple top-level packages discovered in a flat-layout: ['shared', 'clients', 'schemas', 'templates', 'node_modules']`, while `pyproject.toml:1-34` still has no `[tool.setuptools.packages.find]` configuration.
- Correct: Skill alignment remains consistent after the docs correction. The local compatibility shim still delegates to the canonical global skill and keeps the repo-root guard (`.pi/skills/presentation-design/SKILL.md:2-8,18-24`), while the canonical global skill remains repo-bound via its activation guard and still points operators to the same runbook (`C:/Users/AndreiAitzhanov/.pi/agent/skills/bami-presentation-design/SKILL.md:15-34,57-67`).
- Correct: The required smoke test passes after the correction. Re-ran `python -m tools.pptx_gen --schema clients/_sample/deck.json --out .pi/temp/review-r2-smoke.pptx` and `python -m tools.pptx_validate .pi/temp/review-r2-smoke.pptx`; generation produced `built 5 slide(s)` and validation returned `OK: deck conforms to the BAMi design system (5 slides)`.
- Note: Packaging is still unfixed by design in this narrowed revision scope, but the runbook now accurately reflects that state, so the prior blocker is resolved.