## Review
- Correct: Семантический dispatch реализован по плану: `build.py` теперь разворачивает `layout` через `expand_layout()` и рендерит результат в существующий `render_block()` pipeline (`shared/pptx/build.py:174-185`), а семантическая валидация запрещает смешивать `layout` и `blocks` и проверяет известные layout-имена (`shared/pptx/schema.py:97-118`).
- Correct: Базовый scope покрыт кодом и примерами: зарегистрированы `gantt`, `comparison_panel`, `kpi_strip` (`shared/pptx/layouts.py:237-261`), sample deck использует все три layout-а (`clients/_sample/deck.json:83-157`), KoM deck мигрирован на `layout: "gantt"` (`clients/kanadevia-inova-aveva-ue-kom/deck.json:86-140`), reference library добавлена (`templates/media/reference/README.md:1-12`).
- Correct: Тестовое и runtime-подтверждение есть: `gantt` включён в per-kind coverage, есть layout-dispatch test и negative test на mutual exclusivity (`tests/test_blocks_new.py:166-199`, `tests/test_migrations.py:62-81`). Локально проверено: `python -m pytest -q` -> `49 passed`; также вручную собраны и провалидированы `_sample`, `kanadevia-inova-aveva-ue-phase1`, `kanadevia-inova-kom-prototype`, `kanadevia-inova-aveva-ue-kom` — все OK.
- Fixed: нет, review-only.
- Blocker: Пункт плана про **two-level period header band** не выполнен. Реализация `gantt` поддерживает только плоский список `periods` и рисует один ряд period headers (`shared/pptx/blocks.py:990-1055`); схема тоже описывает только `periods[{label,key}]` без модели для верхнего header-level/grouping (`schemas/content-schema.json:76-119`). То есть текущий primitive не может выразить и отрендерить двухуровневую шапку, которая была явно зафиксирована в task 2 плана.
- Note: `README.md` теперь противоречит фактической реализации и может дезориентировать авторов: он всё ещё утверждает, что `layout`/`variant`/`content` "not yet expand" (`README.md:104-107`), что block library содержит 20 kinds (`README.md:111`), что sample deck имеет 5 slides (`README.md:167-168`), что `templates/media/` empty и Pillow не объявлен (`README.md:192-197`), хотя layout dispatch уже live (`shared/pptx/build.py:174-185`), `gantt` добавлен (`shared/pptx/blocks.py:987-1204`), sample deck now 9 slides (`clients/_sample/deck.json:1-180`), reference assets есть (`templates/media/reference/README.md:1-12`), а `pillow` уже в зависимостях (`pyproject.toml:8-14`).