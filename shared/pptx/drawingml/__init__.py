"""Shared DrawingML/native-shape primitives for family-specific renderers.

Each function in this package returns a list of created ``BaseShape`` objects.
All coordinates are in inches (grid space), consistent with ``blocks.py``.
"""
