## Review
- Correct: Canonical/global skill and local compatibility shim are aligned. The global skill is renamed to `bami-presentation-design` and keeps execution repo-bound via an explicit guard (`C:/Users/AndreiAitzhanov/.pi/agent/skills/bami-presentation-design/SKILL.md:16-35,59-70`). The repo-local shim preserves the old name and forwards users to the canonical one (`.pi/skills/presentation-design/SKILL.md:1-34`).
- Correct: README and architecture docs match the approved narrowed scope: they present `bami-content-fabric` as the target identity, keep the current production scope presentation-only, and describe broader domains as future work (`README.md:1-83`, `docs/architecture/technical-description.md:24-78`).
- Correct: The current presentation workflow still works after the rename-related edits. Verified with `python -m tools.pptx_gen --schema clients/_sample/deck.json --out .pi/temp/review-rename-check.pptx` and `python -m tools.pptx_validate .pi/temp/review-rename-check.pptx`, which produced a 5-slide deck and returned `OK`.
- Blocker: `docs/runbooks/generate-deck.md:14-15` now tells users to install with `pip install -e .`, but packaging was explicitly deferred and the command still fails. Reproduced with `python -m pip install -e . --dry-run`, which errors with `Multiple top-level packages discovered in a flat-layout: ['shared', 'clients', 'schemas', 'templates', 'node_modules']` while `pyproject.toml:1-34` still lacks the package-discovery fix. This makes the updated runbook inaccurate. Either remove that install instruction for now or include the packaging fix in scope.
- Note: The rename is still documented as a transition, not a completed physical rename (`README.md:5-8,71-75`, `docs/architecture/technical-description.md:40-56`), and the active workspace path remains `presentation-framework`. If the approved override intended an actual on-disk repo/folder rename, that part is still deferred.
- Note: Full workspace `python -m pytest -q` is not currently clean in this checkout because collection hits unrelated files (`tests/test_blocks_new.py:15` and `shared/pptx/mermaid_render.py:124` via `tests/test_mermaid_render.py`). The tracked baseline suite does pass when run explicitly: `python -m pytest -q tests/test_build_e2e.py tests/test_chrome.py tests/test_clone.py tests/test_validator.py` → `15 passed`.
