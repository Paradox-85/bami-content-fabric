## Review
- Correct:
  - F3 — Resolved. `tests/test_media_library.py:55-136` adds 7 smoke tests for manifest creation, archive refusal, signoff+archive, rerun idempotence, SVG render, `qa_ready`, and group representatives. I ran `python -m pytest -q` myself: **57 passed**.
  - F4 — Resolved. `scripts/media_library.py:527-541` now writes `## Group representatives`, and the regenerated artifact contains it with chosen reps listed at `templates/media/reference/library/_qa/classification-review.md:45-51`.
  - Real-state artifacts match the claimed rerun: `templates/media/reference/library/_qa/qa-report.md:7-15` shows **76 discovered / 76 converted / 0 failed / qa_ready true**; `templates/media/reference/library/_qa/manifest.json:10-18` and `templates/media/reference/library/_qa/manifest.json:2445` show **76 total**, `qa_signoff: false`, `qa_ready: true`. A direct spot-check also returned `archived=0` and no `"archived": true` entries. Running `python scripts/media_library.py archive` now fails with the expected sign-off message.
  - The path-root refactor is internally consistent for regenerated state: `templates/media/reference/library/_qa/manifest.json:3` stores root as `"."`, entries are media-root-relative (`templates/media/reference/library/_qa/manifest.json:21-22`), and both `convert()` and `archive()` resolve through `ROOT / entry[...]` (`scripts/media_library.py:552-555`, `scripts/media_library.py:778-787`).

- Fixed:
  - Не применялось; review-only.

- Blocker:
  - F1 — Partially-resolved. The unsigned path is blocked in the current manifest (`scripts/media_library.py:764-768`), but the sign-off is only a sticky boolean set by `signoff()` (`scripts/media_library.py:793-803`). Later pipeline mutations (`convert()`, `finalize()`, `qa()`) save the manifest without clearing that flag (`scripts/media_library.py:576`, `scripts/media_library.py:634`, `scripts/media_library.py:754-756`), and `archive()` re-checks only the boolean (`scripts/media_library.py:763-768`). I reproduced this in a temp media root: after `signoff()`, deleting the source and rerunning `convert()` still allowed `archive()` to run. So the human QA gate can still be reused against stale state.

- Note:
  - F2 — Partially-resolved. The original Cairo-specific misdiagnosis is gone for the current corpus: deps now point to `resvg-py` (`pyproject.toml:21-24`), rendering uses resvg first and Pillow normalization (`scripts/media_library.py:370-396`), `qa-report.md` has **0 FAILED** at `templates/media/reference/library/_qa/qa-report.md:7-15`, and I confirmed **0** occurrences of `"not installed"`. But the promised “accurate tri-state error” is still incomplete: resvg import still swallows every exception (`scripts/media_library.py:25-28`), and `_svg_unavailable_message()` returns a 1-tuple instead of a string (`scripts/media_library.py:363-367`; verified at runtime). If both engines are unavailable, the fallback failure text will still be malformed / potentially misleading.
  - New issue — Note. Stale conversion metadata is not cleared on rerun failure. On conversion failure, `convert()` sets `openability="failed"` but leaves prior `staging_path` / `converted_path` intact (`scripts/media_library.py:564-575`), while `qa()` counts “converted successfully” by `converted_path` only (`scripts/media_library.py:643-647`). I reproduced this in a temp root: after one successful run, deleting the source and rerunning `convert()` made QA report `converted successfully: 1` and `failed/unprocessable: 1` simultaneously. This makes rerun/stale-file reporting inaccurate.
  - F3 — Resolved, but the new suite does not cover either stale-signoff invalidation or stale-conversion cleanup, which is why both regressions above slipped through.

- Overall verdict: **revise**. F3/F4 are fixed and the current real-state artifacts are clean, but F1 is only partially fixed because stale `qa_signoff` can still unlock `archive()`. The SVG diagnostic cleanup also needs a small follow-up to make the unavailable-engine path actually accurate.