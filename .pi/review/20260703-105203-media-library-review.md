## Review
- Correct: `pyproject.toml:16-25` добавляет отдельную optional-группу `media`, а `templates/media/reference/README.md:18-29` сохраняет плоскую benchmark-конвенцию и явно разводит её с новым каталогом `library/`. Сам CLI покрывает заявленные этапы `inventory/classify/convert/finalize/qa/archive` (`scripts/media_library.py:351-718`), а артефакты действительно сгенерированы: `templates/media/reference/library/README.md:1-24`, `templates/media/reference/library/_qa/qa-report.md:7-10`.
- Fixed: не применялось; это review-only.
- Blocker: обязательный QA sign-off перед архивированием не реализован. `inventory()` создаёт manifest с `"qa_signoff": false` (`scripts/media_library.py:394-407`, `templates/media/reference/library/_qa/manifest.json:17`), `qa()` этот флаг ни разу не меняет (`scripts/media_library.py:560-675`), а `archive()` проверяет только `review_flag`/`--force` (`scripts/media_library.py:678-703`). При этом архивирование уже произошло при `qa_signoff: false`: например, у первого успешно обработанного файла уже стоят `archived: true` и `archived_path` (`templates/media/reference/library/_qa/manifest.json:168-176`). Это нарушает D4 и задачи 9-10 из плана («origins only after QA sign-off»).
- Note: диагностика SVG-блокера вводит в заблуждение. Ошибки импорта `cairosvg` проглатываются широким `except Exception` (`scripts/media_library.py:23-26`), после чего `render_svg_to_png()` всегда сообщает, что пакет «не установлен» (`scripts/media_library.py:314-316`). Именно эта формулировка попала в QA-отчёт (`templates/media/reference/library/_qa/qa-report.md:14-21`). На этой машине реальная причина другая: `import cairosvg` падает с `OSError` из-за отсутствующего Cairo runtime DLL, поэтому текущий отчёт ведёт к неверному remediation path.
- Note: автоматических тестов на новый pipeline не добавлено — поиск по `tests/` не нашёл упоминаний `media_library`. Для такого объёма файловых операций в `scripts/media_library.py:351-718` не хватает хотя бы smoke-тестов на manifest generation, archive gating и rerun behavior.
- Note: review-артефакт по family groups показывает только non-representative варианты, но не выводит выбранный representative как отдельный объект на ручную проверку (`scripts/media_library.py:423-444`). Если задача 5 подразумевала явный human-review именно выбора representative, текущий gate неполный.