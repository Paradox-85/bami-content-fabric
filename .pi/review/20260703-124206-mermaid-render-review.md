## Review
- Correct:
  - C1 — Resolved / Note — `shared/pptx/build.py` не содержит Mermaid-логики (`grep -ic mermaid shared/pptx/build.py == 0` проверено), а диспетчеризация остаётся общей: `render_block(...)` вызывается по обычному циклу `for block in ...` в `shared/pptx/build.py:151-185`.
  - C2 — Resolved / Note — `tools/pptx_validate/cli.py` не содержит Mermaid-логики (`grep -ic mermaid tools/pptx_validate/cli.py == 0` проверено). Примерный deck собран и валидирован успешно: `python -m tools.pptx_gen --schema clients/example-mermaid-architecture-deck.json --out .pi/temp/mermaid-ex.pptx` → 0, `python -m tools.pptx_validate .pi/temp/mermaid-ex.pptx` → 0.
  - C4 — Resolved / Note — Mermaid внедрён только в `add_image`; ветка `flow` отдельная и не связана с Mermaid: `shared/pptx/blocks.py:351-363`, `shared/pptx/blocks.py:693-715`.
  - C6 — Resolved / Note — Fail-loud реализован корректно: отсутствие бинаря, non-zero exit, timeout и пустой output поднимают `MermaidRenderError` в `shared/pptx/mermaid_render.py:118-158`; Windows `.cmd` shim ищется в `shared/pptx/mermaid_render.py:49-67`.
  - C7 — Resolved / Note — В `add_image` добавлена одна минимальная ветка, которая переписывает `b["src"]` в строковый PNG-path и дальше падает в неизменённый путь расчёта placement/contain-fit: `shared/pptx/blocks.py:351-372`.
  - Подтверждены ключевые требования реализации: sha256-кэш по `definition + scale` (`shared/pptx/mermaid_render.py:95-102`), `--scale 3 -b white` и `timeout=120` (`shared/pptx/mermaid_render.py:126-136`), backward-compatible schema `src.oneOf[string | {mermaid}]` (`schemas/content-schema.json:41-53`), и документация в `SKILL.md` с decision rule + brand-clash limitation (`.pi/skills/presentation-design/SKILL.md:124-146`).
  - Тесты и E2E-проверки прошли: `python -m pytest -q` → 64 passed; `python -m pytest -q tests/test_mermaid_render.py` → 5 passed. Повторная сборка example-deck переиспользует кэш: целевой файл `.pi/mermaid-cache/4c26f6809d77f32f.png` сохранил тот же `mtime`, а число `*.png` в `.pi/mermaid-cache/` не выросло.
- Blocker:
  - C5 — Not-resolved / Blocker — Изменён существующий `clients/_sample/deck.json`, хотя по плану и constraint он должен был остаться нетронутым. Текущее содержимое уже включает новые слайды/поля вне Mermaid-задачи (`clients/_sample/deck.json:46-145`); `git diff -- clients` показывает 105 вставок в этом файле.
- Note:
  - C3 — Partially-resolved / Note — Mermaid сам по себе действительно не вводит новый `kind`; используется существующий `image` + `src.oneOf` (`schemas/content-schema.json:36-53`, `shared/pptx/blocks.py:344-363`). Но текущее дерево не scope-clean: в нём одновременно присутствуют unrelated surface expansions (`section_divider`, `layout`, capability-driven dispatch) в `schemas/content-schema.json:21-36` и `shared/pptx/build.py:158-185`, поэтому constraint «no new capability flag / template archetype added» не доказывается для branch state целиком.
  - Новый issue / Note — Интеграционный тест слишком слабый: `tests/test_mermaid_render.py:171-177` проверяет только `len(pic_shapes) >= 1`. Это не доказывает наличие Mermaid-изображения, потому что content slide и без image-блока уже содержит picture shapes из шаблона; я локально собрал deck без image и получил 2 picture shape на content slide.
  - Новый issue / Note — На раннем сбое записи temp `.mmd` есть утечка временного файла: `shared/pptx/mermaid_render.py:108-115` поднимает `MermaidRenderError`, но cleanup из `finally` ещё не активен для этого пути.
  - Новый issue / Note — Кэш не защищён от конкурентного рендера одинаковой диаграммы: при одновременных cache-miss два процесса/потока могут параллельно писать в один и тот же `cache_path`, т.к. нет lock/atomic rename (`shared/pptx/mermaid_render.py:100-105`, `shared/pptx/mermaid_render.py:126-164`).

Overall verdict: revise.
