# Implementation Summary — 20260703-221212-bami-fabric-r2

Executed by `worker` (revision task for the reviewed corrections).

## Scope executed

Applied the reviewed corrections from session `20260703-221212-bami-fabric-r2`:

1. Fix the `pip install -e .` blocker in `docs/runbooks/generate-deck.md`.
2. Re-verify the renamed/global skill setup remains aligned after the docs correction.
3. Re-run the generator/validator smoke test after the correction.
4. Save this implementation summary to `.pi/implementation/20260703-221212-bami-fabric-r2-impl.md`.

(No scope broadening beyond the review findings.)

## What changed

### 1. Docs correction — `docs/runbooks/generate-deck.md`

**Before:** The Prerequisites section claimed:
```
(install: `pip install -e .`).
```

**After:** Replaced with a truthful temporary prerequisite note:
```
(install manually: `pip install python-pptx==1.0.2 pyyaml jsonschema click`).
⚠️ `pip install -e .` does NOT work in this repository yet — the packaging
layout (`[tool.setuptools.packages.find]`) has not been configured. Use the
manual `pip install` command above instead, and run `python -m tools.pptx_gen`
(or `python -m tools.pptx_validate`) from the repository root.
```

**Verification:** Confirmed `pip install -e .` fails with:
```
error: Multiple top-level packages discovered in a flat-layout: ['shared', 'clients', 'schemas', 'templates', 'node_modules'].
```
This is because `pyproject.toml` has no `[tool.setuptools.packages.find]` directive. The correction is accurate.

### 2. Skill setup alignment — verified unchanged

- **Local shim** (`.pi/skills/presentation-design/SKILL.md`): references repo layout requirements (`tools/pptx_gen/cli.py`, `templates/template.pptx`) — no mention of `pip install -e .` or packaging. The docs correction does not affect the shim. Remains aligned.

- **Global canonical skill** (`bami-presentation-design`): lists `python-pptx, pyyaml, jsonschema, click` as dependency hints — does not prescribe *how* to install them. No alignment issue. Remains aligned.

### 3. Smoke test — generator + validator pass

```
$ python -m tools.pptx_gen --schema clients/_sample/deck.json --out /tmp/test_branded.pptx
built 5 slide(s) -> C:\Temp\pi\test_branded.pptx (pruned 8 reference slide(s))

$ python -m tools.pptx_validate /tmp/test_branded.pptx
OK: deck conforms to the BAMi design system (5 slides)
```

Both commands ran successfully from the repository root after the docs correction, confirming the workflow is intact.

## Files changed

| File | Action |
|---|---|
| `docs/runbooks/generate-deck.md` | Corrected — replaced `pip install -e .` with truthful manual install instruction and packaging note |
| `.pi/implementation/20260703-221212-bami-fabric-r2-impl.md` | Created — this summary |

## Verification performed

1. Re-read `docs/runbooks/generate-deck.md` after edit — confirmed the `pip install -e .` line is gone and replaced with the correct warning + manual instruction.
2. Re-read `.pi/skills/presentation-design/SKILL.md` — no changes needed, skill setup unaffected.
3. Re-read `bami-presentation-design` canonical skill — no changes needed.
4. Manually ran `pip install -e .` — confirmed it fails with the flat-layout discovery error (accurate warning).
5. Ran `python -m tools.pptx_gen` on sample deck — exit 0, "built 5 slide(s)".
6. Ran `python -m tools.pptx_validate` on generated output — exit 0, "OK: deck conforms to the BAMi design system (5 slides)".

## Residual risks / notes

1. **Packaging still broken.** `pip install -e .` will continue to fail until `pyproject.toml` gains a `[tool.setuptools.packages.find]` (or equivalent) directive. The docs correction keeps users from hitting this error blindly.
2. **Manual dependency management.** Users must `pip install` the four dependencies manually. This is documented but slightly less convenient than a one-command editable install. Fixing packaging is out of scope for this revision.
3. **No regression in the generator/validator.** The smoke test confirms the current workflow is unaffected by the docs edit.

## Outcome

The single reviewed correction was applied to the runbook. The skill alignment was verified as unaffected. The generator/validator smoke test passes. The implementation summary is saved.
