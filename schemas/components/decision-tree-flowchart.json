{
  "id": "decision-tree-flowchart",
  "vue_component": "DecisionTreeFlowchart",
  "category_id": "decision-tree-flowchart",
  "title": "Decision tree / flowchart",
  "description": "Flowchart with nodes (decision, process, start/end) and directed edges with optional labels.",
  "current_runtime_kind": "diagram",
  "implemented": true,
  "props": [
    {
      "name": "nodes",
      "type": "Array",
      "required": true,
      "default": null,
      "description": "Nodes: [{id, text, type? ('process'|'decision'|'start'|'end'), x?, y?}]."
    },
    {
      "name": "edges",
      "type": "Array",
      "required": true,
      "default": null,
      "description": "Directed edges: [{from, to, label?}]. from/to reference node ids."
    },
    {
      "name": "accent",
      "type": "String",
      "required": false,
      "default": "primary",
      "enum": ["primary", "primary_dark", "positive", "warning", "neutral"],
      "description": "Color token for decision nodes and edge arrows."
    }
  ]
}
